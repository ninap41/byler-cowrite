// GENERATED by scripts/build.mjs from client/ — edit the .ts source, not this file.
import { esc, safeColor, miniAvatar } from "../util.js";
function chatMessageHtml(m) {
  const col = safeColor(m.color);
  const sys = "sys" in m && m.sys === true;
  const host = !sys && "host" in m && (m.host || m.isHost);
  return (sys ? "" : miniAvatar(m)) + `<span class="cn" style="color:${col}">${esc(m.name)}</span>` + (host ? `<span class="cn-host">(host)</span>` : "") + esc(m.text);
}
export {
  chatMessageHtml
};
