// GENERATED by scripts/build.mjs from client/ — edit the .ts source, not this file.
function reconnectOutcome(res, socketId, prevHostId) {
  if (!res?.ok) return { kind: "gone" };
  if ("pending" in res && res.pending) return { kind: "pending" };
  return { kind: "seated", myId: socketId, hostId: ("hostId" in res ? res.hostId : void 0) ?? prevHostId };
}
function adoptSeatId(writers, myId, userId) {
  if (!Array.isArray(writers) || userId == null) return null;
  const list = writers;
  if (list.some((w) => w.id === myId)) return null;
  const mine = list.find((w) => w.userId === userId && w.connected !== false);
  return mine ? mine.id : null;
}
export {
  adoptSeatId,
  reconnectOutcome
};
