// GENERATED by scripts/build.mjs from client/ — edit the .ts source, not this file.
function countdownView(st, now = Date.now()) {
  if (st.paused) {
    if (st.turnSeconds === 0) return { text: "⏸", left: 0, paused: true, low: false, expired: false };
    const left2 = Math.ceil((st.remaining || 0) / 1e3);
    return { text: "⏸ " + left2 + "s", left: left2, paused: true, low: false, expired: false };
  }
  if (!st.deadline) return { text: "∞", left: Infinity, paused: false, low: false, expired: false };
  const left = Math.max(0, Math.ceil((st.deadline - now) / 1e3));
  return { text: left + "s", left, paused: false, low: left <= 10, expired: left <= 0 };
}
export {
  countdownView
};
